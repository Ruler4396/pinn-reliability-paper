"""
Rewrite Introduction (Section 1 + 1.1) and Methods (Sections 2-3)
using Wang et al. 2020 style analysis.
"""
import re

with open("paper_manuscript_zh.md", "r", encoding="utf-8") as f:
    content = f.read()

# ── Find section boundaries ──
idx_intro_start = content.index("### 1. 引言")
idx_intro_end = content.index("### 2. 一般问题定义")

# ── New Introduction (Section 1 + 1.1) ──
new_intro = """### 1. 引言

物理信息神经网络在数据稀缺而控制方程已知的场重建问题中取得了显著进展。从流体力学中的速度场反演到传热问题中的温度场重构，PINN 将物理约束——偏微分方程残差和边界条件——作为损失的组成部分直接编码进神经网络训练[1]。然而，在几乎所有这些成功案例中，实验使用的观测数据量远大于实际工程中可获得的传感器数量，且噪声水平通常被控制在较低范围内。一个更基础的问题尚未得到系统回答：当观测点逐步减少、噪声逐步增大时，PINN 的预测何时从可接受变为不可接受？更重要的是，这种退化在不同性质的 PDE 系统上是否遵循相同的规律？

理解这一问题需要回答三个子问题。第一，在观测数量和噪声强度构成的二维空间中，PINN 的可靠性是否会形成清晰可分析的结构。第二，单一误差指标（如相对 $L_2$ 误差）是否足以描述退化状态——如果模型在误差增大之前就已经表现出物理约束违反或解形态畸变，我们需要额外的诊断维度。第三，不同 PDE 系统——例如平滑椭圆方程、耦合流动、反应-扩散前沿和非线性对流-扩散——是否具有不同形态的可靠性边界。

现有研究为回答这些问题提供了重要基础，但仍留下关键空白。

**训练困难与失效机制。** PINN 的训练不稳定性是一个公认的问题。Krishnapriyan 等[8] 在 Burgers 方程和反应-扩散方程上系统实验表明，PINN 的失效并非随机，而是与 PDE 参数和网络初始化存在规律性关系。Rathore 等[9] 从损失景观的角度分析了训练困难，指出多目标优化中不同损失项的刚度不平衡是收敛失败的主要原因。Wang 等[10] 从神经切线核（NTK）理论出发，揭示了 PINN 对不同频率分量的不均匀收敛速率，证明了 PINN 同样存在谱偏差。这些工作聚焦于"为什么训练不收敛"以及"如何改进训练以促收敛"，但未回答一个与之正交的问题：即便训练确实收敛了，在给定的稀疏度和噪声水平下，模型的预测是否可以信任？

**稀疏数据流场重建。** 另一条研究脉络证明了 PINN 在有限数据下的重建能力。Hosseini 和 Shiri[18] 展示了从稀疏传感器测量重建圆柱绕流速度场，Arzani 等[19] 从稀疏壁面测量反演近壁面血流，Zobeiry 和 Humfeld[20] 在使用带噪传感器数据的传热问题中验证了 PINN 的可行性。Liu 等[21] 结合卷积自编码器从稀疏测量进行流场重建。这些工作展示了 PINN 在特定场景下的有效性，但每个研究限于单一物理系统，无法回答：所观察到的失效特征是该 PDE 独有的，还是跨 PDE 共有的？

**基准评测与不确定性量化。** Hao 等[17] 的 PINNacle 基准在 20 余种 PDE 上系统评测了多种 PINN 变体的求解精度。该工作覆盖了广泛的方程类型，但每个方程仅测试了固定数据设定，未系统扫描观测数量和噪声强度构成的退化空间。贝叶斯 PINN[15] 和 Dropout-based 方法[16] 通过推断参数后验来估计预测不确定性，但这些方法关注"一次预测的不确定性有多大"，而非"在连续观测退化过程中可靠性边界如何演变"。

**本文的工作。** 上述三组工作——训练失效分析、稀疏重建、基准评测——的共同特点是：它们或是改进训练算法，或是在单一 PDE 上测试，或是研究不确定性估计。尚缺乏一项在固定协议下、跨越多个 PDE 系统的系统性可靠性退化研究。

本文填补了这一空白。我们不对训练算法做任何改进，而是固定一个统一的最小化训练协议（相同的网络架构、损失权重、训练轮数），在二维全因子空间（观测数 × 噪声强度）中扫描四个性质不同的 PDE 系统。具体地，本文做了三件事：

（1）提出四个互补的评估维度——物理约束违反、训练稳定性、数值精度和结构保真度——构成可靠性状态。单一误差只能回答"偏离了多远"，而这四个维度分别回答"方程是否满足""训练是否平稳""数值是否接近"和"形态是否保持"。

（2）在二维条件空间中通过全因子矩阵扫描和多种子复现实验，定位每个系统的可靠性边界位置和形态。特别地，我们不仅报告边界的平均位置，还报告越界率的 Wilson 置信区间和跨种子排序稳定性，以区分确定性的"硬边界"和统计性的"概率边界"。

（3）比较四个系统的边界形态，发现它们沿一个清晰的梯度排列：Stokes-Poiseuille 呈现窄而规则的边界，Fisher-KPP 呈现中等宽度的过渡带，Burgers 呈现宽临界带和显著的种子敏感性。这一梯度在基线不稳定度、种子方差和平均跨越率三个定量指标上均成立。

此外，本文试探性地检验了可靠性信息能否指导训练策略的选择。结果确认不加引导的区域感知策略不可靠，但如何利用诊断信息稳定改进训练仍是一个开放问题。这部分仅作为探索性补充。

表 1. 近邻研究方向与本文定位。

| 方向 | 代表工作 | 与本文的关系 |
| --- | --- | --- |
| 训练困难与失效 | Krishnapriyan et al. [8], Wang et al. [10], Rathore et al. [9] | 训练稳定性作为诊断维度之一；不涉及训练算法改进 |
| 稀疏数据流场重建 | Hosseini & Shiri [18], Arzani et al. [19], Zobeiry & Humfeld [20] | 单场景 → 四系统跨方程比较 |
| PINN 基准评测 | PINNacle [17] | 更小规模，但显式扫描退化空间 + 多种子统计 |
| 不确定性量化 | Bayesian PINN [15], Dropout-based [16] | 互补但不重叠；本文关注边界形态而非预测区间 |
| 本文 | — | 固定协议 + 二维扫描 + 四维评估 + 跨 PDE 边界比较 |

"""

# Replace Intro + Related Work
content = content[:idx_intro_start] + new_intro + "\n" + content[idx_intro_end:]
print("Section 1 rewritten")

# ── Find Methods section boundaries ──
idx_sec2 = content.index("### 2. 一般问题定义")
idx_sec4 = content.index("### 4. 物理系统实例化")

# ── New Methods (Sections 2-3) ──
new_methods = """### 2. 一般问题定义

#### 2.1 观测模型与训练范式

设待求解的 PDE 定义在域 $\\Omega$ 上，通式表示为 $\\mathcal{F}(u)=0$，边界条件为 $\\mathcal{B}(u)=0$。观测数据为有限个离散点的测量值 $y_i = u^*(z_i) + \\epsilon_i,\\ i=1,\\dots,N_{\\mathrm{obs}}$，其中 $z_i$ 为空间或时空坐标，$u^*$ 为真实解，$\\epsilon_i$ 为观测噪声。本文关注 $N_{\\mathrm{obs}}$ 逐步减少、噪声 $\\sigma$ 逐步增大时 PINN 预测状态的变化。

PINN 通过一个神经网络 $\\hat{u}(\\cdot; \\theta)$ 逼近 $u^*$，最小化以下组合损失：

$$\\mathcal{L}(\\theta) = \\lambda_d \\mathcal{L}_{\\mathrm{data}} + \\lambda_p \\mathcal{L}_{\\mathrm{phys}} + \\lambda_b \\mathcal{L}_{\\mathrm{bc}}$$

其中三项分别约束网络输出与观测数据的一致性、预测解对 PDE 的满足程度、以及边界/初值条件的符合程度。这一形式是标准 PINN 训练范式的直接实例化[1]。本文不对此范式做任何修改——不用自适应采样、不引入自适应权重、不改变网络结构。设计意图是：在训练算法完全固定的条件下，隔离出 PDE 性质本身对可靠性边界的影响。

#### 2.2 四个可靠性维度

仅用相对 $L_2$ 误差 $\\mathrm{rel}_2 = \\|\\hat{u} - u^*\\|_2 / \\|u^*\\|_2$ 不足以描述 PINN 的退化状态。考虑以下情况：模型在所有观测点上精确拟合，但中间区域违反了 PDE；或者训练损失收敛到低值，但最后 50 轮持续震荡；或者整体误差很小，但解的某个关键剖面形状发生了偏移。每种情况对应不同的可靠性威胁，需要不同的检测指标。

我们从四个维度评估 PINN 在给定观测条件下的状态：

（1）**物理约束违反**：PDE 残差的均方根和边界/初值残差的均方根。这两个指标度量预测解是否符合控制方程和边界条件——回答"方程是否满足"。

（2）**训练稳定性**：训练末段（最后 10 轮）损失的标准差，以及最终损失相对于初始损失的比率。这两个指标度量训练过程是否平稳——回答"训练是否稳定"。

（3）**数值精度**：预测解相对于参考解的相对 $L_2$ 误差。回答"数值上差多少"。

（4）**结构保真度**：预测解与参考解在关键剖面形状上的余弦相似度（$1 - \\cos\\theta$）。对 Poisson，取 $y=0.5$ 中线剖面；对 Stokes-Poiseuille，取 $x=0.5$ 处主速度剖面；对 Fisher-KPP 和 Burgers，取终止时刻剖面。这个维度回答"解的形态是否保持"——它与逐点误差相关但不完全重叠：一个解可以在整体幅值上准确但形态扭曲，或反之。

这四个维度可以用训练过程直接获得（物理约束和训练稳定性）或依赖参考解（数值精度和结构保真度）。在有参考解的基准实验中四个维度均可计算；在缺少参考解的场景中，可观测维度仍可使用。

#### 2.3 从原始指标到无量纲得分

不同维度的原始指标量级差异巨大（例如，Burgers 方程的 PDE 残差 RMS 约为 $10^{-2}$，而 Poisson 方程的残差 RMS 约为 $10^{-1}$），无法直接比较。我们采用案例内分位数校准：对每个案例的每个指标，以 Coarse Matrix 中选定格点（跨过 $1.5\\times$ 基线误差的格点，或若不存在则取 top-quartile 格点）上的 $Q_{15}$ 和 $Q_{85}$ 分位点作为"良好"和"失效"锚点，通过 logistic 映射将原始指标转换为 $[0,1]$ 的无量纲得分：

$$s_j = \\frac{1}{1 + \\exp(-k_j \\cdot (m_j - f_j))}, \\quad k_j = \\frac{\\ln 19}{g_j - f_j}$$

其中 $g_j, f_j$ 分别为良好和失效锚点，$\\ln 19$ 保证 $s(g_j)=0.95, s(f_j)=0.05$。这个映射不改变指标在同案例内的相对排序，只对尺度做归一化。

**Remark 2.1.** 校准分数仅用于同一案例内的排序和主导维度识别。跨系统比较需要回到原始未校准指标。我们在正文中同时报告原始误差和校准后得分，并在附录图 A7 中给出原始量纲的跨系统对照。

对每个维度，若有多个子指标则取几何平均（如 $D_{\\mathrm{phy}} = \\sqrt{s_{\\mathrm{phys}} \\cdot s_{\\mathrm{bc}}}$）。综合分数取四个维度得分的算术平均，等权。此外，我们还计算对数平均 $R_{\\log} = \\exp(\\frac{1}{4}\\sum \\ln D_d)$ 作为稳健性对照——对数平均对最低分维度更敏感，可以避免某一个维度的高分掩盖另一个维度的低分。

#### 2.4 主导维度

对每个观测条件，定义**主导维度**为四个得分中的最小者：$d^* = \\arg\\min_d D_d$。主导维度是这个条件在当前协议下首先退化的维度。这个定义是描述性的——它指示哪个方面最先出现问题，不声称找到了退化的根本原因。

#### 2.5 全因子扫描协议

**设计。** 对每个 PDE 系统，我们执行一个两因子全因子实验。因子 A 为观测点数 $N_{\\mathrm{obs}}$，因子 B 为噪声标准差 $\\sigma$。粗扫描在 6 个观测级别（256, 128, 64, 32, 16, 8）和 5 个噪声级别（0, 0.05, 0.10, 0.15, 0.20）上共 30 个格点，用作初步相图绘制和锚点校准。粗扫描中每个格点运行一次。

在粗扫描识别的边界附近，执行精化扫描（更多观测级别，如 24, 12, 10, 6）以定位边界走向。精化扫描仍为单种子。

在精化扫描确定的边界关键区域，执行概率边界扫描：每个格点使用 3-5 个种子，计算越界率的 95% Wilson 置信区间。这用于区分确定性边界和统计性边界。

**Remark 2.2.** 三种扫描的格点密度和种子数各不相同。正文中的相图来自粗扫描或精化扫描，概率边界讨论和越界率表格来自概率边界扫描。图中明确标注数据来源。

**统一训练配置。** 四个系统使用完全相同的网络架构（$3\\times 64$ tanh MLP）、优化器（Adam, lr=$10^{-3}$）、损失权重（data=10, physics=1, boundary=10）和训练轮数。Poisson、Stokes-Poiseuille 和 Burgers 使用 300 轮，Fisher-KPP 使用 500 轮（因其收敛到可比精度需要更多轮次）。观测点、配点和边界点的基准预算为 $256/2048/256$，评估网格为 $51\\times 51$。

**可控性。** 我们另设训练预算控制实验（$1000/4096/512$），以及 four stronger baselines（更大网络容量、重新平衡损失权重、残差自适应重采样、自适应损失权重），以验证边界形态是否因协议参数变化而消失。如果更强配置下三个系统的边界形态差异仍然保持，则这种差异是稳健的系统特征，而非特定协议的人为产物。

**阈值定义。** 我们将基线误差的 $1.5\\times$ 作为一种操作性参考边界，用于在二维条件空间中定位"可接受"和"退化"的大致分界。这个倍数是一个实用的选择，不是什么理论阈值。正文同时检查了 $1.25\\times$ 和 $2.0\\times$ 下的边界变化，表明阈值会影响边界绝对位置，但不改变三个系统之间的相对分布。

**多案例比较。** 四个 PDE 系统被选中以覆盖不同的数学结构：泊松方程（稳态椭圆型，单变量，平滑）、Stokes-Poiseuille 流（稳态，多变量耦合，强边界约束）、Fisher-KPP 方程（瞬态，反应-扩散，前沿传播）、黏性 Burgers 方程（瞬态，非线性对流-扩散，可发展陡峭梯度）。四个系统均有解析解，因此观测数据、边界条件和评估标签均可精确计算。

"""

# Replace Methods sections
content = content[:idx_sec2] + new_methods + "\n" + content[idx_sec4:]
print("Sections 2-3 rewritten")

with open("paper_manuscript_zh.md", "w", encoding="utf-8") as f:
    f.write(content)

print(f"Done. Output: {len(content)} chars")
